export const tem1=[{
    "type": "object",
    "properties": {
        "name": {
            "title": "${data[0].Address.City}",
            "type": "string"
        },
        "description": {
            "title": "${data[0].Address.City}",
            "type": "string"
        },
        "providerType": {
            "title": "${data[0].Address.City}",
            "type": "string"
        }
    }
},
{
    "type": "object",
    "properties": {
        "name": {
            "title": "${data[1].Address.City}",
            "type": "string"
        },
        "description": {
            "title": "${data[1].Address.City}",
            "type": "string"
        },
        "providerType": {
            "title": "${data[1].Address.City}",
            "type": "string"
        }
    }
},
]
export const tem2=[{
    "type": "object",
    "properties": {
        "name": {
            "title": "${data[0].Address.City}",
            "type": "string"
        },
        "description": {
            "title": "${data[0].Address.City}",
            "type": "string"
        },
        "providerType": {
            "title": "${data[0].Address.City}",
            "type": "string"
        }
    }
}
]

